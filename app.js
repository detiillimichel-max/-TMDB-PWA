const grid=document.querySelector('#grid'),count=document.querySelector('#count'),updated=document.querySelector('#updated'),modal=document.querySelector('#modal');let catalog=[],active='all';
async function load(){try{const r=await fetch('data/catalog.json',{cache:'no-store'});if(!r.ok)throw Error();const d=await r.json();catalog=d.items||[];updated.textContent='Atualizado em '+(d.updated_at?new Date(d.updated_at).toLocaleString('pt-BR'):'data não informada');render()}catch(e){updated.textContent='Catálogo aguardando atualização.';grid.innerHTML='<div class="empty">Execute o GitHub Action depois de configurar o TMDB_TOKEN.</div>'}}
function render(){const a=catalog.filter(x=>active==='all'||x.type===active);count.textContent=a.length+' títulos';grid.innerHTML=a.map(x=>'<article class="card" data-id="'+x.id+'"><img class="poster" loading="lazy" src="'+esc(x.poster)+'" alt="'+esc(x.title)+'"><div class="card-body"><div class="title">'+esc(x.title)+'</div><div class="meta"><span>'+(x.type==='tv'?'Série':'Filme')+'</span><span>'+esc(x.release_date||'')+'</span></div>'+(x.providers||[]).slice(0,3).map(p=>'<span class="provider">'+esc(p.name)+'</span>').join('')+'</div></article>').join('')||'<div class="empty">Nenhum título encontrado.</div>';document.querySelectorAll('.card').forEach(c=>c.onclick=()=>openDetail(c.dataset.id))}
function openDetail(id){const x=catalog.find(v=>String(v.id)===String(id));if(!x)return;detailPoster.src=x.poster;detailPoster.alt=x.title;detailType.textContent=x.type==='tv'?'SÉRIE':'FILME';detailTitle.textContent=x.title;detailDate.textContent=x.release_date?'Estreia: '+new Date(x.release_date+'T12:00:00').toLocaleDateString('pt-BR'):'';detailOverview.textContent=x.overview||'Sinopse não disponível.';detailProviders.innerHTML=(x.providers||[]).map(p=>'<span class="provider">'+esc(p.name)+'</span>').join('');tmdbLink.href=x.tmdb_url||'https://www.themoviedb.org/';modal.hidden=false;document.body.style.overflow='hidden'}
function close(){modal.hidden=true;document.body.style.overflow=''}function esc(s=''){return String(s).replace(/[&<>"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]))}
document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('active'));b.classList.add('active');active=b.dataset.type;render()});document.querySelectorAll('[data-close]').forEach(x=>x.onclick=close);document.addEventListener('keydown',e=>e.key==='Escape'&&close());
let deferred;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferred=e;installBtn.hidden=false});installBtn.onclick=async()=>{if(!deferred)return;deferred.prompt();await deferred.userChoice;deferred=null;installBtn.hidden=true};if('serviceWorker'in navigator)navigator.serviceWorker.register('service-worker.js');load();